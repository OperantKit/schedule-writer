# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
build/
dist/
wheels/

# venv
.venv/
venv/
env/

# Test / coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.tox/

# Local scratch (planning drafts, persona reviews, LLM brainstorms)
.local/

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# direnv (the .envrc itself is tracked, but cached state is not)
.direnv/

# Project-local PLAN drafts
PLAN.md
.venv-fresh/

# Generated standalone HTML artifacts (output of `schedule-writer html` / `blocks`)
/schedule-writer.html
/schedule-writer-blocks.html
/blocks.html

# uv is prohibited by the upstream monorepo rules; defensively gitignore the
# lockfile so an accidental `uv` invocation does not leak into a commit.
uv.lock
