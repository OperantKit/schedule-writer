# Defensive activate: only source if .venv already exists.
# Do NOT use direnv layout (python/pyenv/poetry); the runtime is managed by mise
# and the venv is created with `mise exec -- python -m venv .venv`.
[ -f .venv/bin/activate ] && source .venv/bin/activate
